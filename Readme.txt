1. please add flow.mat into the file and use the load_sequence_color.m in my uploaded file because it was changed 
2. Change the path of the pictures and then run test.m 
3. type a number in the ranng of [1, 72] to choose the starting image.
4. left-click on a series of locations in the image to mark the path.
5. right-click to end drawing
6. Output images are saved as <result/numberOfSequence.jpg>
7. if you want to get a slower motion, you can change the parameter 'Steps'. The bigger 'Steps', the slower motion.